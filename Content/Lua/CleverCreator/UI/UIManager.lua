function C_UIManager_CreateWidget(Widget)
    -- print("C_UIManager_CreateWidget", Widget)
    -- Widget:AddToViewport(0)
end

function C_UIManager_Init(C_UIManager)
    -- UIManager = class("UIManager", C_UIManager)
    -- print("c_uim", C_UIManager)
    -- local uim = getmetatable(C_UIManager)
    -- local idx = uim.__index
    -- uim.__index  = function(uimanager, key)
    --     print('uim', uimanager, key)
    --     return function(umgr, p)
    --         print('uim.func', p)
    --         local func = idx(uimanager, key)
            
    --         return func(umgr, p, "")
    --     end
    -- end
    
    -- UIManager = C_UIManager
    -- print("C_UIManager_Init", UIManager)
    -- for k,v in pairs(uim) do
    --     print(k,v)
    -- end
end